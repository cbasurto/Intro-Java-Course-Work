//cbasurto indexOf method for hw2

public class indexOf
{

    public static int findIndex(String s, int c) {
        int charac = c;
	String s1 = word;
	while (charac < 0)
        {
	    string.char
    }

    public static boolean compareFunc(String s1, String s2)
    {
        int r1 = s1.indexOf(s2);
        int r2 = findIndex(s1.toCharArray(), s2.toCharArray());
        boolean ret = (r1==r2);
        System.out.println(ret + " for '" + s1 + "' '" + s2 + "' -> " + r1 + " " + r2);
        return ret;
    }
}
